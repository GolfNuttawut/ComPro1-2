#include <stdio.h>
#include <limits.h>
void main()
{
    int N,K,MAX = INT_MIN;
    scanf("%d%d",&N,&K);
    int x[N];
    for(int i=0;i<N;i++)
    {
        scanf("%d",&x[i]);
    }
    for(i=0;i<)
}
